# =============================================
# MAU Scraper v2 — Environment Configuration
# =============================================
# Copy this file to .env and fill in your keys.
# You need at least ONE API key (Anthropic or OpenAI).

# Anthropic API — https://console.anthropic.com/
# Required if using --api haiku or --api sonnet
ANTHROPIC_API_KEY=your-key-here

# OpenAI API — https://platform.openai.com/api-keys
# Required if using --api o4-mini, gpt-4o-mini, or gpt-4.1-mini
OPENAI_API_KEY=your-key-here

# Whisper model size (optional, default: medium)
# Options: tiny, base, small, medium, large
#   tiny   — fastest, least accurate
#   base   — fast, good for testing
#   small  — balanced
#   medium — recommended for production runs
#   large  — most accurate, slowest
WHISPER_MODEL=medium
